# NexusDocs Environment Configuration
# =====================================
# Copy this file to .env.local for development

# =====================================
# SECURITY WARNING
# =====================================
# NEVER commit .env or .env.local to version control
# NEVER expose these keys in client-side code
# The OPENAI_API_KEY must only be accessed in server-side code (API routes)

# =====================================
# Mock Mode (Development)
# =====================================
# Set to "true" to use mock data instead of real API calls
NEXT_PUBLIC_MOCK_MODE=true

# =====================================
# OpenAI Configuration (Server-Side Only)
# =====================================
# This key is loaded ONLY on the server in API routes
# It is NEVER exposed to the browser
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_READ_MODEL=gpt-4o
OPENAI_WRITE_MODEL=gpt-5.1

# =====================================
# GitHub OAuth (Server-Side Only)
# =====================================
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:3000/api/auth/github/callback

# =====================================
# Application
# =====================================
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =====================================
# Database (MySQL)
# =====================================
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: mysql://nexusdocs:password@localhost:3306/nexusdocs
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/DATABASE"
